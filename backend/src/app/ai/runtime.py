"""AI runtime shim.

Phase 1: LangGraph is still implemented under `src/flow` and wired via
`src/api/routes/langgraph.py`.

As we migrate, graph runtime and triggers will move under `src.app.ai`.
"""
